apiVersion: v1
kind: Service
metadata:
  name: safeview-web
  labels:
    app.kubernetes.io/name: safeview-web
    app.kubernetes.io/part-of: safeview
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: safeview-web
  ports:
    - name: http
      port: 80
      targetPort: http
